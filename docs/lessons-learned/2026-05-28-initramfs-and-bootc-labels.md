# Lessons Learned: VM smoke test 2026-05-28 → 2026-05-29

The first end-to-end VM boot of the published Margine image surfaced
**four bugs** in the build pipeline. All were runtime-only — none
caught by `bash -n`, `shellcheck`, `python3 -m py_compile`,
`yaml.safe_load`, or buildah's own validation. All shipped to `:stable`
before being detected by a human booting a VM.

This document records each bug, what made it invisible until smoke
test, and the durable guardrail we added so it cannot recur silently.

## Bug 1 — `dracut --kver X --regenerate-all` rejected silently

**Symptom.** Kernel panic at first boot of CachyOS kernel:

    VFS: Cannot open root device "UUID=..." or unknown-block(0,0)
    Kernel panic - not syncing: VFS: Unable to mount root fs

Available partitions listed as raw `vda{1,2,3}` only — no LUKS
mapper, no btrfs. The initramfs had no crypto/btrfs userspace.

**Cause.** `custom-kernel/install.sh` and `build.sh` both called:

    dracut --force --kver "$KVER" --regenerate-all || true

`--kver` and `--regenerate-all` are mutually exclusive. dracut printed
`--regenerate-all cannot be called with a kernel version` and exited 1.
The `|| true` swallowed the exit code, the build marked the step
green, and no initramfs ever got generated by our code. The image
boot used whatever fallback initramfs Bluefin DX's base layer happened
to have, which had been built against Bluefin's kernel and didn't
contain modules for CachyOS-specific drivers / a different module
layout.

**Fix.** Use `dracut --regenerate-all` without `--kver`. Drop the
`|| true` — if dracut fails the image is unbootable, fail loud.

**Durable guardrail.** Layer A check 2 (`initramfs size sanity (>50 MB,
host-only is <30)`) catches "no initramfs / minimal initramfs" cases.
Layer A check 3 (`critical kernel features`) verifies the relevant
features (dm-crypt, dm-mod, btrfs, virtio_blk) are present either as
loadable modules OR as built-in kernel config.

## Bug 2 — Host-only initramfs

**Symptom.** Same panic as Bug 1, even after Bug 1 was fixed.

**Cause.** `dracut --regenerate-all` runs in host-only mode by default.
The "host" in our case is the GitHub Actions runner's build container:
no LUKS device, no btrfs root, no virtio_blk. dracut correctly omits
modules that aren't needed by the build environment — but those are
the very modules the deployment environment needs.

**Fix.** Force generic initramfs:

    dracut --force --no-hostonly --no-hostonly-cmdline --regenerate-all

Also dropped `/etc/dracut.conf.d/01-margine-no-hostonly.conf` so any
later dracut invocation (e.g. user-triggered `rpm-ostree initramfs
--enable`) keeps the same posture.

**Durable guardrail.** Layer A checks 2 and 3 (same as Bug 1).

## Bug 3 — Initramfs written to `/boot/` instead of `/usr/lib/modules/<KVER>/`

**Symptom.** Same panic. Inspection of the published image showed
initramfs WAS present at `/boot/initramfs-7.0.8-cachyos1.fc44.x86_64.img`,
sized 303 MB, with crypto+btrfs+virtio_blk modules embedded. Yet boot
still failed.

**Cause.** bootc/ostree at deployment time picks initramfs from
`/usr/lib/modules/<KVER>/initramfs.img`, NOT from `/boot/`. dracut's
default output is `/boot/initramfs-<KVER>.img` (the
traditional/Anaconda location). Without an initramfs at the
ostree-expected path, ostree either auto-generates a host-only one at
deploy time, or skips, and the kernel panics on the user's actual
LUKS-encrypted disk.

Verified by comparing with Bluefin DX base image, which has its
initramfs at `/usr/lib/modules/<KVER>/initramfs.img`.

**Fix.** Pass the output path as a positional argument to dracut:

    for kver_dir in /usr/lib/modules/*/; do
      kver=$(basename "$kver_dir")
      dracut --force --no-hostonly --no-hostonly-cmdline \
          --kver "$kver" \
          "${kver_dir}initramfs.img"
    done

**Durable guardrail.** Layer A check 1 (`/usr/lib/modules/*/initramfs.img
exists`) catches "no initramfs at bootc-canonical path".

## Bug 4 — `ostree.linux` OCI label not updated after kernel swap

**Symptom.** After all three initramfs bugs were fixed, image boots
past the initramfs but drops to dracut emergency shell with:

    Failed to start initrd-switch-root.service

Investigation from the emergency shell showed:

  - Bootloader entry "Margine 44" referenced deployment hash
    `a96ed40c1a0ec706a787fa58d78607b4a833090d3f3686106bc5b30c86b459c1`
  - On-disk `/sysroot/.../ostree/deploy/default/deploy/` contained
    `16d0802e3ae...` and `440f96cce527...` — different hashes
  - Both Bluefin and Margine bootloader entries pointed at deployment
    dirs that didn't exist on disk
  - Switch-root looks for `/ostree/boot.1/default/<hash>/0` — the path
    encoded in the bootloader entry — and that path didn't resolve

**Cause.** Bluefin DX labels its image with
`ostree.linux=<bluefin-kernel-version>`. Our `custom-kernel/install.sh`
swaps the kernel files for kernel-cachyos but leaves the inherited OCI
label intact. rpm-ostree and bootc both consult `ostree.linux` at
deploy time to:

  1. Decide which kernel version to wire into the bootloader entry
  2. Look up kernel files at `/usr/lib/modules/<label>/`

Pointed at a kernel version that doesn't exist (Bluefin's), ostree
creates the deployment directory but doesn't finish placing all the
expected boot artifacts. The bootloader entry generation then uses a
hash derived from one source, the actual deploy dir name is derived
from another, and the two end up different.

**Fix.** New workflow step (`Fix ostree.linux label to actual installed
kernel`) runs after `Build image` and before Layer A. Reads the actual
kernel from `/usr/lib/modules/` inside the image and updates the label
on every tag via `buildah config --label ostree.linux=<kver>`.

**Durable guardrail.** Layer A check 3b verifies the invariant on
every future build.

## Meta-lessons

### `|| true` is a smell

Every `|| true` in an image build must be individually justified in a
comment: *why is it OK to ignore the failure of THIS specific command?*

For our `v4l2loopback` build it's justified (best-effort kmod, doesn't
block boot). For `dracut` it never was (failure means unbootable
image). Bug 1 hid behind a `|| true` for months. We removed all
unjustified ones during this iteration.

### "Image builds green" ≠ "image boots"

CI before this iteration ran `bash -n`, `shellcheck`, `py_compile`,
`yaml.safe_load`, buildah validation, and `bootc container lint`.
All four bugs above passed all of those. The first boot of a real VM
on real hardware (or vTPM-backed QEMU) is the ONLY way to surface
runtime bugs in initramfs generation, bootloader entry creation, and
OCI label semantics.

**Action items added:**
- Layer A (image-internals static inspection from inside `podman run`)
  — added 2026-05-28, gates push. ~30s per build.
- Layer B (qcow2 + QEMU smoke-boot in CI) — pending. Will detect
  runtime issues before they reach `:stable`. Estimated 5-8 min per
  build.

### Inherited OCI labels are dangerous

When you build `FROM ghcr.io/ublue-os/bluefin-dx:stable`, you inherit
**all** of Bluefin's OCI labels — including ones (like `ostree.linux`)
that describe Bluefin's content, not yours. Any image that materially
changes what those labels describe MUST overwrite them.

Future labels to watch:
- `ostree.linux` — kernel version (fixed)
- `ostree.commit` — currently inherits Bluefin's. We haven't seen
  this cause issues but it's technically stale. Worth investigating.
- `dev.hhd.rechunk.info` — Bluefin's rechunk package manifest.
  Cosmetic but inaccurate for us.
- `org.opencontainers.image.{title,version,source}` — we DO overwrite
  these correctly via the workflow metadata step.

### State corruption can be triggered by repeated rebases

During smoke-test recovery we ran `rpm-ostree cleanup -rp` several
times. This deletes deployment dirs but apparently doesn't always
synchronize bootloader entries — we ended up with bootloader entries
pointing at hashes no longer on disk. The reproducibility of this is
unclear; the root cause was Bug 4 (label mismatch). A clean
`rpm-ostree cleanup -rmpb` from a sane Bluefin base + fresh rebase
should recover. If we ever see this happen on a CI-verified image, it
becomes a separate bug to file upstream against rpm-ostree.
