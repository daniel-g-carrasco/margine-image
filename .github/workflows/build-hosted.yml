---
# A/B alternative to build.yml: same image build pipeline but on a
# GitHub-hosted runner (ubuntu-24.04) instead of the self-hosted PVE
# builder VM.
#
# Why: the self-hosted builder has had multiple freezes (cache=writeback
# + I/O caps + degraded ZFS rpool) that take the whole PVE host down.
# GitHub-hosted is "container that wakes up when a job arrives and
# shuts down after" — exactly the on-demand model we want.
#
# Trigger: workflow_dispatch only (manual). This way we don't burn
# GHA minutes on every push during the evaluation phase.
#
# Output: publishes to `:candidate-hosted` (and `:candidate-hosted.<date>`).
# Kept separate from `:candidate` of the self-hosted pipeline so we can
# compare digests and behavior side-by-side. Once we're satisfied, we
# either promote this workflow's output to `:candidate` directly or
# decommission build.yml.

name: Build Margine image (hosted runner)

on:
  workflow_dispatch:

env:
  IMAGE_REGISTRY: "ghcr.io/${{ github.repository_owner }}"
  IMAGE_NAME: margine
  IMAGE_DESC: "Margine — Bluefin DX + CachyOS signed kernel + Margine deltas"
  IMAGE_KEYWORDS: "bootc,ublue,bluefin,cachyos,margine"
  CANDIDATE_TAG: "candidate-hosted"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}
  cancel-in-progress: true

jobs:
  build_push:
    name: Build, sign kernel, push (hosted)
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
      - name: Maximize build space
        # ubuntu-24.04 has ~14 GiB of free disk by default; we need
        # ~30+ GiB for the buildah cache + base image + Margine layers
        # + rechunk staging. This action removes Android/Haskell/.NET/
        # Swift/CodeQL/GHC pre-installed bundles, freeing ~30 GiB.
        uses: ublue-os/remove-unwanted-software@cc0becac701cf642c8f0a6613bbdaf5dc36b259e # v9
        with:
          remove-codeql: true

      - name: Checkout
        uses: actions/checkout@v4
        with:
          # Need the helpers under scripts/ from margine-fedora-atomic
          # via build.sh fetch; the checkout of THIS repo is just for
          # Containerfile + build_files/.
          fetch-depth: 1

      - name: Pre-build login to GHCR
        # Authenticated pull of ghcr.io/ublue-os/bluefin-dx:stable
        # (5000 req/h vs 100 anonymous).
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | podman login ghcr.io \
            -u "${{ github.actor }}" --password-stdin

      - name: Stage MOK secrets for BuildKit
        env:
          MOK_KEY:      ${{ secrets.MOK_KEY }}
          MOK_CERT:     ${{ secrets.MOK_CERT }}
          MOK_PASSWORD: ${{ secrets.MOK_PASSWORD }}
        run: |
          mkdir -p /tmp/margine-secrets
          chmod 700 /tmp/margine-secrets
          printf '%s' "$MOK_KEY"      > /tmp/margine-secrets/MOK.key
          printf '%s' "$MOK_CERT"     > /tmp/margine-secrets/MOK.pem
          printf '%s' "$MOK_PASSWORD" > /tmp/margine-secrets/mok-password
          chmod 600 /tmp/margine-secrets/*

      - name: Get current date
        id: date
        run: |
          echo "date=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> $GITHUB_OUTPUT
          echo "ymd=$(date -u +%Y%m%d)" >> $GITHUB_OUTPUT

      - name: Image metadata
        uses: docker/metadata-action@v5
        id: metadata
        with:
          tags: |
            type=raw,value=${{ env.CANDIDATE_TAG }}
            type=raw,value=${{ env.CANDIDATE_TAG }}.{{date 'YYYYMMDD'}}
          labels: |
            io.artifacthub.package.readme-url=https://raw.githubusercontent.com/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}/${{ github.sha }}/README.md
            org.opencontainers.image.created=${{ steps.date.outputs.date }}
            org.opencontainers.image.description=${{ env.IMAGE_DESC }}
            org.opencontainers.image.source=https://github.com/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}/blob/${{ github.sha }}/Containerfile
            org.opencontainers.image.title=${{ env.IMAGE_NAME }}
            org.opencontainers.image.url=https://github.com/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}/tree/${{ github.sha }}
            org.opencontainers.image.vendor=${{ github.repository_owner }}
            org.opencontainers.image.version=${{ env.CANDIDATE_TAG }}.{{date 'YYYYMMDD'}}
            io.artifacthub.package.deprecated=false
            io.artifacthub.package.keywords=${{ env.IMAGE_KEYWORDS }}
            io.artifacthub.package.license=Apache-2.0
            io.artifacthub.package.prerelease=false
            containers.bootc=1
          sep-tags: " "
          sep-annotations: " "

      - name: Build image (kernel signing via BuildKit secrets)
        id: build_image
        uses: redhat-actions/buildah-build@v2
        with:
          containerfiles: |
            ./Containerfile
          image: ${{ env.IMAGE_NAME }}
          tags: ${{ steps.metadata.outputs.tags }}
          labels: ${{ steps.metadata.outputs.labels }}
          oci: false
          extra-args: |
            --secret=id=mok-key,src=/tmp/margine-secrets/MOK.key
            --secret=id=mok-cert,src=/tmp/margine-secrets/MOK.pem
            --secret=id=mok-password,src=/tmp/margine-secrets/mok-password

      - name: Move built image to root storage for rechunk
        # rechunk uses `sudo podman create` and looks in root storage;
        # buildah leaves the image in the gha user's rootless storage.
        # Use oci-archive intermediate (same approach as build.yml).
        run: |
          set -euo pipefail
          IMG="${{ env.IMAGE_NAME }}:${{ env.CANDIDATE_TAG }}"
          ARCHIVE="/tmp/${{ env.IMAGE_NAME }}.oci.tar"
          rm -f "$ARCHIVE"
          podman save --format oci-archive --output "$ARCHIVE" "localhost/$IMG"
          sudo skopeo copy --retry-times 3 --preserve-digests \
            "oci-archive:$ARCHIVE" \
            "containers-storage:$IMG"
          sudo podman image exists "$IMG"
          sudo podman images
          rm -f "$ARCHIVE"

      - name: ReChunk image
        id: rechunk
        uses: hhd-dev/rechunk@v1.2.4
        with:
          ref: ${{ env.IMAGE_NAME }}:${{ env.CANDIDATE_TAG }}
          version: ${{ env.CANDIDATE_TAG }}.${{ steps.date.outputs.ymd }}
          labels: |
            org.opencontainers.image.title=${{ env.IMAGE_NAME }}
            org.opencontainers.image.description=${{ env.IMAGE_DESC }}
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.url=https://github.com/${{ github.repository }}
            org.opencontainers.image.vendor=${{ github.repository_owner }}
            org.opencontainers.image.licenses=Apache-2.0
            io.artifacthub.package.keywords=${{ env.IMAGE_KEYWORDS }}
            io.artifacthub.package.license=Apache-2.0
            containers.bootc=1
          revision: ${{ github.sha }}

      - name: Login to GHCR
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | sudo skopeo login \
            -u "${{ github.actor }}" --password-stdin ghcr.io

      - name: Push rechunked image to GHCR
        env:
          IMAGE_REGISTRY: ${{ env.IMAGE_REGISTRY }}
          IMAGE_NAME: ${{ env.IMAGE_NAME }}
        run: |
          set -euo pipefail
          IMG_FULL="${IMAGE_REGISTRY,,}/${IMAGE_NAME,,}"
          for tag in ${{ steps.metadata.outputs.tags }}; do
            sudo skopeo copy --retry-times 3 \
              --dest-creds="${{ github.actor }}:${{ secrets.GITHUB_TOKEN }}" \
              "${{ steps.rechunk.outputs.ref }}" \
              "docker://${IMG_FULL}:${tag}"
          done

      - name: Install Cosign
        uses: sigstore/cosign-installer@v3

      - name: Sign image with cosign
        env:
          COSIGN_PRIVATE_KEY: ${{ secrets.COSIGN_PRIVATE_KEY }}
          COSIGN_PASSWORD: ""
        run: |
          IMAGE_FULL="${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_NAME }}"
          for tag in ${{ steps.metadata.outputs.tags }}; do
            cosign sign -y --key env://COSIGN_PRIVATE_KEY "${IMAGE_FULL}:${tag}"
          done

      - name: Wipe staged secrets
        if: always()
        run: rm -rf /tmp/margine-secrets

      - name: Notify ntfy (build outcome — hosted)
        if: always()
        env:
          NTFY_URL: ${{ secrets.NTFY_TOPIC_URL }}
        run: |
          if [[ -z "${NTFY_URL:-}" ]]; then
            echo "NTFY_TOPIC_URL secret not set, skipping notification."
            exit 0
          fi
          STATUS="${{ job.status }}"
          RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          case "$STATUS" in
            success)
              TITLE="Hosted-runner build OK → :candidate-hosted pushed"
              PRIO="default"
              TAGS="white_check_mark,cloud"
              MSG="GHA-hosted parallel pipeline pushed candidate-hosted. Run #${{ github.run_number }}. Compare against self-hosted :candidate to decide migration."
              ;;
            failure)
              TITLE="Hosted-runner build FAILED"
              PRIO="high"
              TAGS="x,cloud"
              MSG="GHA-hosted build failed. See run for details."
              ;;
            *)
              TITLE="Hosted-runner build $STATUS"
              PRIO="default"
              TAGS="warning,cloud"
              MSG=""
              ;;
          esac
          curl --silent --show-error --fail \
            -H "Title: ${TITLE}" \
            -H "Priority: ${PRIO}" \
            -H "Tags: ${TAGS}" \
            -H "Click: ${RUN_URL}" \
            -d "$MSG" \
            "$NTFY_URL" || echo "ntfy notify failed (non-fatal)"
