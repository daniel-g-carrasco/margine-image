# Margine Phone Camera: the virtual webcam device exists from boot so
# the GUI never needs privileges. With exclusive_caps=1 (modprobe.d)
# apps only see it while a producer is streaming.
v4l2loopback
