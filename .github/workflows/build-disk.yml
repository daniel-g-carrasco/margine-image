---
# Margine ISO + qcow2 builder.
#
# Builds Anaconda installer ISO (single-step Margine install) and
# raw qcow2 (VM-ready disk image) from the currently-promoted
# ghcr.io/<owner>/margine:stable. ISO and qcow2 are then rsync'd
# to the `edge` VM (110) on PVE1 which serves them at
# https://files.the-empty.place/margine/ via Caddy.
#
# Runs on the same self-hosted runner as build.yml. We rely on the
# same storage-health/cleanup primitives.
#
# Cadence: manual trigger only for now (workflow_dispatch). Once
# `:stable` updates daily, you can switch to workflow_run after
# smoke-boot promotion to auto-publish a fresh ISO every release.

name: Build disk images

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - './disk_config/disk.toml'
      - './disk_config/iso-gnome.toml'
      - './.github/workflows/build-disk.yml'

env:
  IMAGE_NAME: margine
  IMAGE_REGISTRY: "ghcr.io/${{ github.repository_owner }}"
  DEFAULT_TAG: "stable"
  BIB_IMAGE: "quay.io/centos-bootc/bootc-image-builder:latest"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}
  cancel-in-progress: true

jobs:
  build:
    name: Build ISO + qcow2 → publish to edge
    runs-on: [self-hosted, margine-builder]
    timeout-minutes: 75
    strategy:
      fail-fast: false
      matrix:
        disk-type: ["qcow2", "anaconda-iso"]
    permissions:
      contents: read
      packages: read
      id-token: write

    steps:
      - name: Prepare environment
        run: |
          USER_UID=$(id -u)
          USER_GID=$(id -g)
          DISK_TYPE=$(echo "${{ matrix.disk-type }}" | tr ' ' '-')
          echo "IMAGE_REGISTRY=${IMAGE_REGISTRY,,}" >> ${GITHUB_ENV}
          echo "IMAGE_NAME=${IMAGE_NAME,,}" >> ${GITHUB_ENV}
          echo "DISK_TYPE=${DISK_TYPE}" >> ${GITHUB_ENV}
          echo "USER_UID=${USER_UID}" >> ${GITHUB_ENV}
          echo "USER_GID=${USER_GID}" >> ${GITHUB_ENV}

      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Pre-build login to GHCR (for base-image pull)
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | timeout 30 podman login ghcr.io \
            -u "${{ github.actor }}" --password-stdin

      - name: Pre-pull bootc-image-builder (resilient to quay.io 5xx)
        # quay.io occasionally returns 5xx for minutes at a time
        # (observed 2026-06-01: a Cloudflare/quay outage that the
        # downstream action surfaced only as a single failed pull).
        # Retry explicitly with exponential backoff so the image is
        # in the local store by the time the action runs; the action
        # then finds it cached and proceeds.
        run: |
          set -uo pipefail
          for attempt in 1 2 3 4 5 6 7 8; do
            if sudo podman pull "${{ env.BIB_IMAGE }}"; then
              echo "bootc-image-builder pulled on attempt $attempt"
              exit 0
            fi
            backoff=$((attempt * 30))
            echo "pull attempt $attempt failed; sleeping ${backoff}s before retry"
            sleep $backoff
          done
          echo "::error::Unable to pull ${{ env.BIB_IMAGE }} after 8 attempts; quay.io may be down"
          exit 1

      - name: Build disk image
        id: build
        uses: osbuild/bootc-image-builder-action@main
        with:
          builder-image: ${{ env.BIB_IMAGE }}
          config-file: ${{ matrix.disk-type == 'anaconda-iso' && './disk_config/iso-gnome.toml' || './disk_config/disk.toml' }}
          image: ${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.DEFAULT_TAG }}
          chown: ${{ env.USER_UID }}:${{ env.USER_GID }}
          types: ${{ matrix.disk-type }}
          # Margine's :stable inherits from Bluefin DX, which doesn't
          # set the `containers.bootc.rootfs` OCI label; without an
          # explicit rootfs here bootc-image-builder errors out with
          # "DefaultRootFs missing". Bluefin's own ISO uses btrfs and
          # so does Margine's Anaconda installer config (Btrfs default
          # in Anaconda).
          rootfs: btrfs
          additional-args: --use-librepo=True

      - name: List + sha256 outputs
        id: outputs
        run: |
          set -euo pipefail
          OUTDIR="${{ steps.build.outputs.output-directory }}"
          echo "outdir=$OUTDIR" >> "$GITHUB_OUTPUT"
          ls -la "$OUTDIR"
          find "$OUTDIR" -type f | while read -r f; do
            sha256sum "$f" | tee -a "$OUTDIR/SHA256SUMS"
          done
          echo "files=$(find "$OUTDIR" -type f -name '*.iso' -o -name '*.qcow2' | tr '\n' ' ')" >> "$GITHUB_OUTPUT"

      - name: Install Internet Archive CLI (ia)
        if: github.event_name != 'pull_request'
        run: |
          set -euo pipefail
          if ! command -v ia >/dev/null; then
            python3 -m pip install --user --quiet internetarchive
            echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          fi
          ~/.local/bin/ia --version || ia --version

      - name: Upload to Internet Archive (torrent-first distribution)
        # IA auto-generates a BitTorrent .torrent + magnet + 3 HTTP
        # mirrors for everything we upload, and seeds it forever.
        # This is the same pattern Bluefin/Bazzite use to avoid
        # Cloudflare TOS (no large binary content served from origin)
        # and to keep our home-server upload bandwidth free.
        if: github.event_name != 'pull_request' && matrix.disk-type == 'anaconda-iso'
        env:
          IA_S3_ACCESS: ${{ secrets.IA_ACCESS_KEY }}
          IA_S3_SECRET: ${{ secrets.IA_SECRET_KEY }}
        run: |
          set -euo pipefail
          OUTDIR="${{ steps.outputs.outdir }}"
          DATE_TAG="$(date -u +%Y%m%d)"
          IDENTIFIER="margine-${{ matrix.disk-type }}-${DATE_TAG}"
          export IA_CONFIG_FILE=/tmp/ia.ini
          cat > "$IA_CONFIG_FILE" <<EOF
          [s3]
          access = ${IA_S3_ACCESS}
          secret = ${IA_S3_SECRET}
          EOF
          chmod 0600 "$IA_CONFIG_FILE"
          ia --config-file "$IA_CONFIG_FILE" upload "$IDENTIFIER" \
            "$OUTDIR"/*.iso "$OUTDIR"/SHA256SUMS \
            --metadata="mediatype:software" \
            --metadata="collection:opensource" \
            --metadata="title:Margine Linux ISO ${DATE_TAG}" \
            --metadata="description:Margine — Bluefin DX + CachyOS signed kernel + Margine deltas. Built on ${DATE_TAG} from commit ${{ github.sha }}. Source: https://github.com/${{ github.repository }}" \
            --metadata="subject:linux;fedora;bootc;bluefin;cachyos;margine" \
            --metadata="creator:daniel-g-carrasco" \
            --metadata="licenseurl:https://www.apache.org/licenses/LICENSE-2.0"
          echo "identifier=$IDENTIFIER" >> "$GITHUB_OUTPUT"
          echo "Uploaded as https://archive.org/details/$IDENTIFIER"
        id: ia_upload

      - name: Wait for IA to derive (gen torrent + mirrors)
        if: github.event_name != 'pull_request' && matrix.disk-type == 'anaconda-iso'
        timeout-minutes: 25
        env:
          IDENTIFIER: ${{ steps.ia_upload.outputs.identifier }}
        run: |
          set -euo pipefail
          # IA needs a few minutes to derive a .torrent + magnet for
          # the upload. Poll until the *_archive.torrent file shows up.
          for i in $(seq 1 60); do
            sleep 20
            if ia --config-file /tmp/ia.ini list "$IDENTIFIER" 2>/dev/null | grep -q '_archive\.torrent$'; then
              echo "Torrent ready after ~$((i*20))s"
              ia --config-file /tmp/ia.ini list "$IDENTIFIER"
              exit 0
            fi
            echo "  (poll $i) torrent not yet derived, retrying…"
          done
          echo "::warning::IA torrent not derived within 20 min — index will use HTTP mirrors only"

      - name: Generate index.html for files.the-empty.place
        if: github.event_name != 'pull_request' && matrix.disk-type == 'anaconda-iso'
        env:
          IDENTIFIER: ${{ steps.ia_upload.outputs.identifier }}
        run: |
          set -euo pipefail
          OUTDIR="${{ steps.outputs.outdir }}"
          DATE_TAG="$(date -u +%Y%m%d)"
          ISO_NAME=$(basename "$OUTDIR"/*.iso)
          # Magnet URI for the IA-generated torrent. IA URL pattern:
          # https://archive.org/download/<id>/<id>_archive.torrent
          # We construct a magnet directly with the xt= sha1 left empty
          # (most clients will fetch the .torrent from the tr= URL).
          TORRENT_URL="https://archive.org/download/${IDENTIFIER}/${IDENTIFIER}_archive.torrent"
          MIRROR_HTTP="https://archive.org/download/${IDENTIFIER}/${ISO_NAME}"
          cat > "$OUTDIR/index.html" <<HTML
          <!doctype html>
          <html lang="en"><head>
            <meta charset="utf-8">
            <title>Margine ${DATE_TAG} — downloads</title>
            <style>
              body { font: 15px/1.5 ui-sans-serif, system-ui, sans-serif; max-width: 720px; margin: 40px auto; padding: 0 16px; color: #1a1a1a; }
              h1 { font-size: 22px; }
              code { background: #f0f0f0; padding: 2px 6px; border-radius: 4px; font-size: 13px; }
              .pkg { background: #fafafa; border: 1px solid #ddd; border-radius: 8px; padding: 16px 20px; margin-top: 12px; }
              a { color: #1e5fb6; }
              footer { margin-top: 32px; color: #888; font-size: 13px; }
            </style>
          </head><body>
            <h1>Margine ${DATE_TAG}</h1>
            <p>Margine — Bluefin DX + CachyOS signed kernel.
               Build dal commit <code>${{ github.sha }}</code>.</p>

            <div class="pkg">
              <strong>Anaconda installer ISO</strong> · <code>${ISO_NAME}</code><br>
              <p>
                ⤓ <a href="${TORRENT_URL}">Download .torrent</a> (consigliato)<br>
                ⤓ <a href="${MIRROR_HTTP}">Download HTTP diretto</a> (IA mirror)<br>
                🗂 <a href="https://archive.org/details/${IDENTIFIER}">Pagina Internet Archive</a> (tutti i mirror)
              </p>
            </div>

            <p>Hash di verifica: <a href="SHA256SUMS">SHA256SUMS</a></p>

            <footer>
              Pubblicato il $(date -u +%Y-%m-%dT%H:%MZ).
              ISO ospitate da <a href="https://archive.org">Internet Archive</a>;
              questo sito serve solo magnet/HTML.
              Sorgente: <a href="https://github.com/${{ github.repository }}">${{ github.repository }}</a>.
            </footer>
          </body></html>
          HTML

      - name: Publish index + SHA256SUMS to edge over rsync
        # Only the lightweight pieces go to edge: HTML index page,
        # SHA256SUMS, and (for the 7-day fallback) a copy of the ISO.
        # The large binaries' authoritative location is Internet Archive.
        if: github.event_name != 'pull_request'
        run: |
          set -euo pipefail
          OUTDIR="${{ steps.outputs.outdir }}"
          # Ship index, sha, and any small artifact. The ISO/qcow2 go too
          # for the 7-day local fallback (cleanup timer on edge removes
          # them after).
          rsync -rtvz --progress \
            -e "ssh -i /home/gha-runner/.ssh/edge-files-margine -o StrictHostKeyChecking=accept-new" \
            "$OUTDIR"/ \
            edge-files-margine@192.168.2.31:/var/www/files-margine/

      - name: Builder post-build hygiene (self-hosted only)
        if: always() && runner.environment != 'github-hosted'
        timeout-minutes: 3
        run: |
          # Free local copy: edge has it now, no reason to keep ~5 GiB on builder.
          OUTDIR="${{ steps.outputs.outdir }}"
          if [[ -n "$OUTDIR" && -d "$OUTDIR" ]]; then
            rm -rf "$OUTDIR" || true
          fi
          # Sweep tmpfs left-overs and prune cache mounts
          rm -f /var/tmp/margine*.tar /tmp/margine*.tar 2>/dev/null || true
          df -h /

      - name: Notify ntfy (disk-build outcome)
        if: always()
        env:
          NTFY_URL: ${{ secrets.NTFY_TOPIC_URL }}
        run: |
          if [[ -z "${NTFY_URL:-}" ]]; then
            echo "NTFY_TOPIC_URL secret not set, skipping notification."
            exit 0
          fi
          STATUS="${{ job.status }}"
          DISK="${{ matrix.disk-type }}"
          RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          case "$STATUS" in
            success)
              TITLE="Disk image ${DISK} published"
              PRIO="default"
              TAGS="cd,white_check_mark"
              MSG="Torrent + HTTP mirror disponibili su https://files.the-empty.place/"
              ;;
            failure)
              TITLE="Disk image ${DISK} BUILD FAILED"
              PRIO="high"
              TAGS="x,cd"
              MSG="Vedi i log: ${RUN_URL}"
              ;;
            *)
              TITLE="Disk image ${DISK} status: ${STATUS}"
              PRIO="low"
              TAGS="warning"
              MSG=""
              ;;
          esac
          curl --silent --show-error --fail \
            -H "Title: ${TITLE}" \
            -H "Priority: ${PRIO}" \
            -H "Tags: ${TAGS}" \
            -H "Click: ${RUN_URL}" \
            -d "${MSG}" \
            "$NTFY_URL" || echo "ntfy notify failed (non-fatal)"
