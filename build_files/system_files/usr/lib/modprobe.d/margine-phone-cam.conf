# Deterministic virtual camera for the Phone Camera app: /dev/video21,
# discoverable by label. exclusive_caps=1 keeps it invisible to camera
# pickers while idle (capture caps appear only with a live producer).
options v4l2loopback devices=1 video_nr=21 card_label="Margine Phone Cam" exclusive_caps=1
